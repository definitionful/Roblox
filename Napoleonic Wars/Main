if getgenv()._UnloadPreviousInstance then
    pcall(getgenv()._UnloadPreviousInstance)
end

local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/definitionful/Roblox/refs/heads/main/Library"))()
local ThemeManager = loadstring(game:HttpGet("https://raw.githubusercontent.com/definitionful/Roblox/refs/heads/main/Napoleonic%20Wars/ThemeManager"))()
local SaveManager = loadstring(game:HttpGet("https://raw.githubusercontent.com/violin-suzutsuki/LinoriaLib/refs/heads/main/addons/SaveManager.lua"))()

getgenv()._UnloadPreviousInstance = function()
    if Library and not Library.Unloaded then
        Library:Unload()
    end
end

local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")
local LocalPlayer = Players.LocalPlayer

local ProjectileArray = nil
local lastTargetUpdateTime = 0
local lastProjectileScan = 0
local TARGET_UPDATE_INTERVAL = 0.05
local AIM_SPEED = 0.15
local cachedTargets = {}
local Connections = {}
local drawingPropCache = {}
local meleeAuraPoints = {}
local debrisFolder = Workspace:FindFirstChild("Debris")
local silentAimTarget = { part = nil, hitPos = nil, character = nil, pos2D = nil }
local currentMeleeTargetRoot = nil

for _, inst in ipairs(getinstances()) do
    if inst.ClassName == "ModuleScript" and inst.Name == "UtilityIncidentsClient" then
        local success, util = pcall(require, inst)
        if success and type(util) == "table" then
            for key, val in pairs(util) do
                if type(val) == "function" then
                    local originalFunction
                    originalFunction = hookfunction(util[key], newcclosure(function(...)
                        for _, arg in ipairs({...}) do
                            local flag = tostring(arg)
                            if flag == "muzzleChanges" or flag == "rayCurve" or flag == "rayOffset" or flag == "projectileVelocity" or flag == "fudgeTrap" or flag == "dealMeleeIntercept" then
                                return
                            end
                        end
                        return originalFunction(...)
                    end))
                end
            end
        end
    end
end

local function locateProjectileArray()
    for _, item in ipairs(getgc(true)) do
        if type(item) == "table" and rawget(item, 1) and type(rawget(item, 1)) == "table" then
            local proj = rawget(item, 1)
            if type(rawget(proj, "simulate")) == "function" and type(rawget(proj, "destroy")) == "function" and rawget(proj, "velocity") then
                ProjectileArray = item
                return true
            end
        end
    end
    return false
end

local function findModuleBySignature(signatures)
    for _, item in ipairs(getgc(true)) do
        if type(item) == "table" then
            local match = true
            for _, sig in ipairs(signatures) do
                if type(rawget(item, sig)) ~= "function" then
                    match = false
                    break
                end
            end
            if match then return item end
        end
    end
    return nil
end

local WeaponsDamageClient = findModuleBySignature({"dealMelee", "dealMeleeToHorse", "getHorseByModelPart"})
local WeaponsTargetsClient = findModuleBySignature({"hitTarget"})

for _, obj in ipairs(Workspace:GetDescendants()) do
    if obj.Name == "TargetModel" and obj:IsA("Model") then
        cachedTargets[obj] = true
    end
end

table.insert(Connections, Workspace.DescendantAdded:Connect(function(obj)
    if obj.Name == "TargetModel" and obj:IsA("Model") then
        cachedTargets[obj] = true
    end
end))

table.insert(Connections, Workspace.DescendantRemoving:Connect(function(obj)
    if cachedTargets[obj] then
        cachedTargets[obj] = nil
    end
end))

local function setDrawingProp(drawing, prop, value)
    if not drawingPropCache[drawing] then drawingPropCache[drawing] = {} end
    if drawingPropCache[drawing][prop] ~= value then
        drawingPropCache[drawing][prop] = value
        drawing[prop] = value
    end
end

local FOVCircle = Drawing.new("Circle")
setDrawingProp(FOVCircle, "Visible", false)
setDrawingProp(FOVCircle, "Thickness", 1)
setDrawingProp(FOVCircle, "NumSides", 32)
setDrawingProp(FOVCircle, "Filled", false)
setDrawingProp(FOVCircle, "Transparency", 1)
setDrawingProp(FOVCircle, "ZIndex", 2)

local FOVFilledCircle = Drawing.new("Circle")
setDrawingProp(FOVFilledCircle, "Visible", false)
setDrawingProp(FOVFilledCircle, "Thickness", 1)
setDrawingProp(FOVFilledCircle, "NumSides", 32)
setDrawingProp(FOVFilledCircle, "Filled", true)
setDrawingProp(FOVFilledCircle, "Transparency", 1)
setDrawingProp(FOVFilledCircle, "ZIndex", 1)

local TracerLine = Drawing.new("Line")
setDrawingProp(TracerLine, "Visible", false)
setDrawingProp(TracerLine, "Thickness", 1)
setDrawingProp(TracerLine, "ZIndex", 3)

local MeleeRangeLines = {}
for i = 1, 30 do
    local line = Drawing.new("Line")
    setDrawingProp(line, "Thickness", 1)
    setDrawingProp(line, "Transparency", 1)
    setDrawingProp(line, "Visible", false)
    table.insert(MeleeRangeLines, line)
end

local MeleeTargetTracer = Drawing.new("Line")
setDrawingProp(MeleeTargetTracer, "Thickness", 1)
setDrawingProp(MeleeTargetTracer, "Transparency", 1)
setDrawingProp(MeleeTargetTracer, "Visible", false)

local sharedVisibilityParams = RaycastParams.new()
sharedVisibilityParams.FilterType = Enum.RaycastFilterType.Exclude

local Window = Library:CreateWindow({
    Title = 'Napoleonic Wars',
    Center = true,
    AutoShow = true,
    TabPadding = 8,
    MenuFadeTime = 0.2
})

local Toggles = getgenv().Toggles
local Options = getgenv().Options

local Tabs = {
    Combat = Window:AddTab('Combat'),
    Misc = Window:AddTab('Misc'),
    Settings = Window:AddTab('Settings')
}

local SilentAimGroup = Tabs.Combat:AddLeftGroupbox('Silent Aim')
local MeleeGroup = Tabs.Combat:AddLeftGroupbox('Melee Aura')
local FOVGroup = Tabs.Combat:AddRightGroupbox('FOV')
local TracerGroup = Tabs.Combat:AddRightGroupbox('Tracer')
local MiscTabbox = Tabs.Misc:AddLeftTabbox()
local WeaponTab = MiscTabbox:AddTab('Weapon')
local MenuGroup = Tabs.Settings:AddLeftGroupbox('Menu')

SilentAimGroup:AddToggle('SilentAimEnabled', { Text = 'Enable', Default = false }):AddKeyPicker('SilentAimKeybind', { Default = '-', SyncToggleState = true, Mode = 'Toggle', Text = 'Silent Aim' })
SilentAimGroup:AddDropdown('SilentAimHitpart', { Values = { 'HumanoidRootPart', 'Head' }, Default = 1, Multi = false, Text = 'Hitpart' })
SilentAimGroup:AddToggle('StickyAim', { Text = 'Sticky Aim', Default = false })
SilentAimGroup:AddToggle('VisibleCheck', { Text = 'Visible Check', Default = false })
SilentAimGroup:AddToggle('PrioritizeFlagBearers', { Text = 'Prioritize Flag Bearers', Default = false })
SilentAimGroup:AddSlider('SilentAimMaxDist', { Text = 'Max Distance', Default = 4700, Min = 1, Max = 4700, Rounding = 0, Suffix = ' st' })
SilentAimGroup:AddSlider('HitChance', { Text = 'Hit Chance', Default = 100, Min = 0, Max = 100, Rounding = 0, Suffix = '%' })

MeleeGroup:AddToggle('MeleeAuraEnable', { Text = 'Enable', Default = false }):AddKeyPicker('MeleeAuraKeybind', { Default = '-', SyncToggleState = true, Mode = 'Toggle', Text = 'Melee Aura' })
MeleeGroup:AddSlider('MeleeAuraRange', { Text = 'Range', Default = 12, Min = 5, Max = 30, Rounding = 1, Suffix = ' st' })
MeleeGroup:AddSlider('MeleeAuraDelay', { Text = 'Delay', Default = 0.5, Min = 0.5, Max = 1, Rounding = 2, Suffix = ' s' })
MeleeGroup:AddToggle('MeleeAuraPriorityLow', { Text = 'Prioritize Low Health', Default = false }):OnChanged(function()
    if Toggles.MeleeAuraPriorityLow.Value and Toggles.MeleeAuraPriorityHigh.Value then
        Toggles.MeleeAuraPriorityHigh:SetValue(false)
    end
end)
MeleeGroup:AddToggle('MeleeAuraPriorityHigh', { Text = 'Prioritize High Health', Default = false }):OnChanged(function()
    if Toggles.MeleeAuraPriorityHigh.Value and Toggles.MeleeAuraPriorityLow.Value then
        Toggles.MeleeAuraPriorityLow:SetValue(false)
    end
end)
MeleeGroup:AddToggle('CustomDamageToggle', { Text = 'Custom Damage', Default = false })
MeleeGroup:AddSlider('CustomDamageAmount', { Text = 'Damage Amount', Default = 50, Min = 1, Max = 50, Rounding = 0, Suffix = ' HP' })
MeleeGroup:AddToggle('MeleeAuraCircle', { Text = 'Show Range Circle', Default = false }):AddColorPicker('RangeCircleColor', { Default = Color3.new(1, 1, 1), Title = 'Range Circle Color' })
MeleeGroup:AddToggle('MeleeAuraTracer', { Text = 'Show Target Tracer', Default = false }):AddColorPicker('MeleeTracerColor', { Default = Color3.new(1, 0, 0), Title = 'Target Tracer Color' })

FOVGroup:AddToggle('FOVEnabled', { Text = 'Enable', Default = false })
FOVGroup:AddToggle('FOVOutline', { Text = 'Outline', Default = false }):AddColorPicker('FOVOutlineColor', { Default = Library.AccentColor })
FOVGroup:AddToggle('FOVFilled', { Text = 'Filled', Default = false }):AddColorPicker('FOVFilledColor', { Default = Library.AccentColor })
FOVGroup:AddSlider('FOVRadius', { Text = 'Radius', Default = 100, Min = 0, Max = 600, Rounding = 0 })
FOVGroup:AddSlider('FOVThickness', { Text = 'Thickness', Default = 1, Min = 1, Max = 10, Rounding = 0 })
FOVGroup:AddSlider('FOVNumSides', { Text = 'Num Sides', Default = 32, Min = 10, Max = 60, Rounding = 0 })
FOVGroup:AddSlider('FOVFilledTransparency', { Text = 'Filled Transparency', Default = 0.5, Min = 0, Max = 1, Rounding = 2 })

TracerGroup:AddToggle('TracerEnabled', { Text = 'Enable', Default = false }):AddColorPicker('TracerColor', { Default = Library.AccentColor })
TracerGroup:AddSlider('TracerThickness', { Text = 'Thickness', Default = 1, Min = 1, Max = 10, Rounding = 0 })

WeaponTab:AddToggle('NoRecoil', { Text = 'No Recoil', Default = false })
WeaponTab:AddToggle('NoSpread', { Text = 'No Spread', Default = false })
WeaponTab:AddToggle('InstantAim', { Text = 'Instant Aim', Default = false })
WeaponTab:AddToggle('NoSmoke', { Text = 'No Smoke', Default = false })
WeaponTab:AddToggle('NoScreenEffects', { Text = 'No Screen Effects', Default = false })

MenuGroup:AddToggle('WatermarkToggle', { Text = 'Watermark', Default = false }):OnChanged(function()
    Library:SetWatermarkVisibility(Toggles.WatermarkToggle.Value)
end)
MenuGroup:AddToggle('KeybindsToggle', { Text = 'Keybinds', Default = false }):OnChanged(function()
    Library.KeybindFrame.Visible = Toggles.KeybindsToggle.Value
end)

Library:SetWatermark('Napoleonic Wars Client')
Library:SetWatermarkVisibility(false)

ThemeManager:SetLibrary(Library)
SaveManager:SetLibrary(Library)
ThemeManager:SetFolder('Napoleonic Wars')
SaveManager:SetFolder('Napoleonic Wars')
SaveManager:IgnoreThemeSettings()
ThemeManager:ApplyToTab(Tabs.Settings)
SaveManager:BuildConfigSection(Tabs.Settings)

Options.AccentColor:OnChanged(function()
    Options.FOVOutlineColor:SetValueRGB(Options.AccentColor.Value)
    Options.FOVFilledColor:SetValueRGB(Options.AccentColor.Value)
    Options.TracerColor:SetValueRGB(Options.AccentColor.Value)
end)

for _, obj in ipairs(getgc(true)) do
    if type(obj) == "table" then
        if rawget(obj, "ClassName") == "FireActionClient" and rawget(obj, "UpdateReticle") then
            local originalUpdateReticle = obj.UpdateReticle
            obj.UpdateReticle = function(self, progress, ...)
                if Toggles.InstantAim.Value then
                    if self.aiming then
                        self.customAimProgress = math.min(1, (self.customAimProgress or 0) + AIM_SPEED)
                        local smoothProgress = math.sin(self.customAimProgress * (math.pi / 2))
                        self.aimProgress = smoothProgress
                        self.realAimTime = math.max(self.realAimTime or 0, smoothProgress)
                        return originalUpdateReticle(self, smoothProgress, ...)
                    else
                        self.customAimProgress = 0
                        return originalUpdateReticle(self, progress, ...)
                    end
                end
                return originalUpdateReticle(self, progress, ...)
            end
        elseif rawget(obj, "shakeFiredShot") then
            local originalShake = obj.shakeFiredShot
            obj.shakeFiredShot = function(...)
                if Toggles.NoRecoil.Value then return end
                return originalShake(...)
            end
        elseif rawget(obj, "fireBProjectile") then
            local originalFireBProjectile = obj.fireBProjectile
            obj.fireBProjectile = function(velocity, position, weaponValue, spread, selfObj, ...)
                if Toggles.NoSpread.Value and selfObj and type(selfObj) == "table" and selfObj.muzzleCFrame then
                    if Toggles.SilentAimEnabled.Value and silentAimTarget and silentAimTarget.part then
                        return originalFireBProjectile(velocity, position, weaponValue, 0, selfObj, ...)
                    else
                        local straightVelocity = selfObj.muzzleCFrame.LookVector * velocity.Magnitude
                        return originalFireBProjectile(straightVelocity, selfObj.muzzleCFrame.Position, weaponValue, 0, selfObj, ...)
                    end
                end
                return originalFireBProjectile(velocity, position, weaponValue, spread, selfObj, ...)
            end
        end

        if rawget(obj, "addCannonScreenSmoke") and rawget(obj, "projectileLanded") then
            local origCannonSmoke = obj.addCannonScreenSmoke
            obj.addCannonScreenSmoke = function(...)
                if Toggles.NoScreenEffects.Value then return end
                return origCannonSmoke(...)
            end

            local origPlayerFired = obj.playerFiredGun
            obj.playerFiredGun = function(...)
                if Toggles.NoScreenEffects.Value then return end
                return origPlayerFired(...)
            end

            local origNearbyDeath = obj.nearbyDeath
            obj.nearbyDeath = function(...)
                if Toggles.NoScreenEffects.Value then return end
                return origNearbyDeath(...)
            end

            local origProjLanded = obj.projectileLanded
            obj.projectileLanded = function(...)
                if Toggles.NoScreenEffects.Value then return end
                return origProjLanded(...)
            end
        end
    end
end

local function checkVisibility(startPos, endPos, character)
    local direction = endPos - startPos
    local dynamicFilterList = {Workspace.CurrentCamera}
    if LocalPlayer.Character then table.insert(dynamicFilterList, LocalPlayer.Character) end
    if debrisFolder then table.insert(dynamicFilterList, debrisFolder) end

    for _ = 1, 5 do
        sharedVisibilityParams.FilterDescendantsInstances = dynamicFilterList
        local result = Workspace:Raycast(startPos, direction, sharedVisibilityParams)
        if not result then return true end
        if character and result.Instance:IsDescendantOf(character) then return true end
        if result.Instance.Transparency > 0.5 or not result.Instance.CanCollide then
            table.insert(dynamicFilterList, result.Instance)
        else
            return false
        end
    end
    return false
end

local function getClosestCharacterToMouse(mousePos, currentCamera)
    if not currentCamera then return nil, nil, nil, nil end
    local fovEnabled = Toggles.FOVEnabled.Value
    local fovRadiusSq = Options.FOVRadius.Value * Options.FOVRadius.Value
    local maxDistance = Options.SilentAimMaxDist.Value
    
    local entitiesList = {}
    local potentialTargetsList = {}

    local camCFramePos = currentCamera.CFrame.Position
    local hitpartName = Options.SilentAimHitpart.Value or "HumanoidRootPart"

    for _, player in ipairs(Players:GetPlayers()) do
        if player == LocalPlayer then continue end
        if LocalPlayer.Team and player.Team == LocalPlayer.Team then continue end
        local character = player.Character
        if not character then continue end
        local humanoid = character:FindFirstChildOfClass("Humanoid")
        local targetPart = character:FindFirstChild(hitpartName) or character:FindFirstChild("HumanoidRootPart") or character.PrimaryPart
        if not humanoid or not targetPart or humanoid.Health <= 0 then continue end
        if humanoid.MaxHealth > 100 then continue end

        local priority = 3
        if player:GetAttribute("FlagBearer") == true then
            if Toggles.PrioritizeFlagBearers.Value then
                priority = priority + 10
            else
                priority = priority - 5
            end
        end
        table.insert(entitiesList, {character = character, part = targetPart, priority = priority})
    end

    for obj in pairs(cachedTargets) do
        if obj.Parent then
            local targetBag = obj:FindFirstChild("Bag")
            if targetBag then
                table.insert(entitiesList, {character = obj, part = targetBag, priority = 1})
            end
        end
    end

    for i = 1, #entitiesList do
        local entity = entitiesList[i]
        local targetPart = entity.part
        if not targetPart or not targetPart.Parent then continue end
        local point = targetPart.Position
        if (point - camCFramePos).Magnitude > maxDistance then continue end
        local screenPos, onScreen = currentCamera:WorldToViewportPoint(point)
        if not onScreen then continue end
        local px, py = screenPos.X - mousePos.X, screenPos.Y - mousePos.Y
        local distSq = (px * px) + (py * py)
        if fovEnabled and distSq > fovRadiusSq then continue end
        table.insert(potentialTargetsList, {
            distanceSq = distSq, pos2D = Vector2.new(screenPos.X, screenPos.Y),
            part = targetPart, hitPos = point, character = entity.character, priority = entity.priority
        })
    end

    table.sort(potentialTargetsList, function(a, b)
        if a.priority ~= b.priority then
            return a.priority > b.priority
        end
        return a.distanceSq < b.distanceSq
    end)

    for i = 1, #potentialTargetsList do
        local target = potentialTargetsList[i]
        if not Toggles.VisibleCheck.Value or checkVisibility(camCFramePos, target.hitPos, target.character) then
            return target.pos2D, target.part, target.hitPos, target.character
        end
    end

    return nil, nil, nil, nil
end

table.insert(Connections, RunService.RenderStepped:Connect(function()
    local currentCamera = Workspace.CurrentCamera
    if not currentCamera then return end
    local mousePos = UserInputService:GetMouseLocation()
    local silentAimEnabled = Toggles.SilentAimEnabled.Value
    local tracerEnabled = Toggles.TracerEnabled.Value
    local fovEnabled = Toggles.FOVEnabled.Value
    local requireTargeting = silentAimEnabled or tracerEnabled

    if requireTargeting then
        local now = os.clock()
        if now - lastTargetUpdateTime >= TARGET_UPDATE_INTERVAL then
            lastTargetUpdateTime = now
            local keepTarget = false
            if Toggles.StickyAim.Value and silentAimTarget.character then
                local char = silentAimTarget.character
                local hrp = silentAimTarget.part
                local hum = char:FindFirstChildOfClass("Humanoid")
                if char.Parent and hrp and hrp.Parent and (not hum or (hum.Health > 0 and hum.MaxHealth <= 100)) then
                    local point = hrp.Position
                    local camPos = currentCamera.CFrame.Position
                    if (point - camPos).Magnitude <= Options.SilentAimMaxDist.Value then
                        local screenPos, onScreen = currentCamera:WorldToViewportPoint(point)
                        if onScreen then
                            local px, py = screenPos.X - mousePos.X, screenPos.Y - mousePos.Y
                            local distSq = (px * px) + (py * py)
                            local fovRadiusSq = Options.FOVRadius.Value * Options.FOVRadius.Value
                            if (not fovEnabled) or (distSq <= fovRadiusSq) then
                                if not Toggles.VisibleCheck.Value or checkVisibility(camPos, point, char) then
                                    keepTarget = true
                                    silentAimTarget.hitPos = point
                                    silentAimTarget.pos2D = Vector2.new(screenPos.X, screenPos.Y)
                                end
                            end
                        end
                    end
                end
            end
            if not keepTarget then
                local pos2D, hitPart, hitPos, hitChar = getClosestCharacterToMouse(mousePos, currentCamera)
                silentAimTarget.part = hitPart
                silentAimTarget.hitPos = hitPos
                silentAimTarget.character = hitChar
                silentAimTarget.pos2D = pos2D
            end
        end
    else
        silentAimTarget.part = nil
        silentAimTarget.hitPos = nil
        silentAimTarget.character = nil
        silentAimTarget.pos2D = nil
    end

    if fovEnabled then
        local outlineEnabled = Toggles.FOVOutline.Value
        local filledEnabled = Toggles.FOVFilled.Value
        local fovRadius = Options.FOVRadius.Value
        local fovNumSides = Options.FOVNumSides.Value

        setDrawingProp(FOVCircle, "Visible", outlineEnabled)
        setDrawingProp(FOVFilledCircle, "Visible", filledEnabled)

        if outlineEnabled then
            setDrawingProp(FOVCircle, "Position", mousePos)
            setDrawingProp(FOVCircle, "Radius", fovRadius)
            setDrawingProp(FOVCircle, "Thickness", Options.FOVThickness.Value)
            setDrawingProp(FOVCircle, "NumSides", fovNumSides)
            setDrawingProp(FOVCircle, "Color", Options.FOVOutlineColor.Value)
        end
        if filledEnabled then
            setDrawingProp(FOVFilledCircle, "Position", mousePos)
            setDrawingProp(FOVFilledCircle, "Radius", fovRadius)
            setDrawingProp(FOVFilledCircle, "NumSides", fovNumSides)
            setDrawingProp(FOVFilledCircle, "Color", Options.FOVFilledColor.Value)
            setDrawingProp(FOVFilledCircle, "Transparency", Options.FOVFilledTransparency.Value)
        end
    else
        setDrawingProp(FOVCircle, "Visible", false)
        setDrawingProp(FOVFilledCircle, "Visible", false)
    end

    if tracerEnabled and silentAimTarget.pos2D then
        setDrawingProp(TracerLine, "Visible", true)
        setDrawingProp(TracerLine, "From", mousePos)
        setDrawingProp(TracerLine, "To", silentAimTarget.pos2D)
        setDrawingProp(TracerLine, "Color", Options.TracerColor.Value)
        setDrawingProp(TracerLine, "Thickness", Options.TracerThickness.Value)
    else
        setDrawingProp(TracerLine, "Visible", false)
    end

    local char = LocalPlayer.Character
    local root = char and char:FindFirstChild("HumanoidRootPart")

    if Toggles.MeleeAuraEnable.Value and Toggles.MeleeAuraCircle.Value and root then
        local range = Options.MeleeAuraRange.Value
        local rootPos = root.Position - Vector3.new(0, 3, 0)
        local angle = 6.283185307179586 / 30
        for i = 1, 30 do
            local currentAngle = angle * i
            local point3d = rootPos + Vector3.new(math.cos(currentAngle) * range, 0, math.sin(currentAngle) * range)
            local point2d, onScreen = currentCamera:WorldToViewportPoint(point3d)
            if not meleeAuraPoints[i] then meleeAuraPoints[i] = {} end
            meleeAuraPoints[i].pos = Vector2.new(point2d.X, point2d.Y)
            meleeAuraPoints[i].onScreen = onScreen
        end
        for i = 1, 30 do
            local p1 = meleeAuraPoints[i]
            local p2 = meleeAuraPoints[i == 30 and 1 or i + 1]
            local line = MeleeRangeLines[i]
            if p1.onScreen or p2.onScreen then
                setDrawingProp(line, "From", p1.pos)
                setDrawingProp(line, "To", p2.pos)
                setDrawingProp(line, "Color", Options.RangeCircleColor.Value)
                setDrawingProp(line, "Visible", true)
            else
                setDrawingProp(line, "Visible", false)
            end
        end
    else
        for i = 1, 30 do setDrawingProp(MeleeRangeLines[i], "Visible", false) end
    end

    if Toggles.MeleeAuraEnable.Value and Toggles.MeleeAuraTracer.Value and root and currentMeleeTargetRoot and currentMeleeTargetRoot.Parent then
        local rootPos, rootOnScreen = currentCamera:WorldToViewportPoint(root.Position)
        local targetPos, targetOnScreen = currentCamera:WorldToViewportPoint(currentMeleeTargetRoot.Position)
        if rootOnScreen or targetOnScreen then
            setDrawingProp(MeleeTargetTracer, "From", Vector2.new(rootPos.X, rootPos.Y))
            setDrawingProp(MeleeTargetTracer, "To", Vector2.new(targetPos.X, targetPos.Y))
            setDrawingProp(MeleeTargetTracer, "Color", Options.MeleeTracerColor.Value)
            setDrawingProp(MeleeTargetTracer, "Visible", true)
        else
            setDrawingProp(MeleeTargetTracer, "Visible", false)
        end
    else
        setDrawingProp(MeleeTargetTracer, "Visible", false)
    end
end))

table.insert(Connections, RunService.Heartbeat:Connect(function(deltaTime)
    if not ProjectileArray then 
        local now = os.clock()
        if now - lastProjectileScan > 1 then
            lastProjectileScan = now
            locateProjectileArray()
        end
        return 
    end
    if not Toggles.SilentAimEnabled.Value then return end

    local targetPart = silentAimTarget.part
    if not targetPart then return end
    local chance = Options.HitChance.Value

    for _, projectile in pairs(ProjectileArray) do
        if type(projectile) == "table" and projectile.isLocal and projectile.position and projectile.velocity then
            local currentSpeed = math.min(projectile.velocity.Magnitude, 999)
            if currentSpeed > 1 then
                if rawget(projectile, "_silentAimHit") == nil then
                    rawset(projectile, "_silentAimHit", math.random(1, 100) <= chance)
                end
                if rawget(projectile, "_silentAimHit") then
                    local targetDirection = (targetPart.Position - projectile.position).Unit
                    local gravityCompensation = Vector3.new(0, 64, 0) * deltaTime
                    projectile.velocity = (targetDirection * currentSpeed) + gravityCompensation
                end
            end
        end
    end
end))

local auraTask = task.spawn(function()
    while true do
        local delay = Options.MeleeAuraDelay and Options.MeleeAuraDelay.Value or 0.5
        task.wait(delay)

        if not Toggles.MeleeAuraEnable.Value or not WeaponsDamageClient then 
            currentMeleeTargetRoot = nil
            continue 
        end

        local char = LocalPlayer.Character
        local root = char and char:FindFirstChild("HumanoidRootPart")
        local myHum = char and char:FindFirstChildOfClass("Humanoid")
        if not root or not myHum or myHum.Health <= 0 then 
            currentMeleeTargetRoot = nil
            continue 
        end

        local origin = root.Position
        local range = Options.MeleeAuraRange.Value
        local validTargets = {}

        for _, player in ipairs(Players:GetPlayers()) do
            if player ~= LocalPlayer and (not LocalPlayer.Team or player.Team ~= LocalPlayer.Team) then
                local pChar = player.Character
                if pChar then
                    local hum = pChar:FindFirstChildWhichIsA("Humanoid")
                    local targetRoot = pChar:FindFirstChild("HumanoidRootPart")
                    if hum and targetRoot and hum.Health > 0 then
                        if hum.MaxHealth > 100 then continue end
                        local dist = (targetRoot.Position - origin).Magnitude
                        if dist <= range then
                            local priority = 3
                            if player:GetAttribute("FlagBearer") == true then
                                priority = -2
                            end
                            table.insert(validTargets, {
                                Player = player, Humanoid = hum, Root = targetRoot,
                                Health = hum.Health, IsModel = false, Priority = priority
                            })
                        end
                    end
                end
            end
        end

        if WeaponsTargetsClient then
            for obj in pairs(cachedTargets) do
                if obj.Parent then
                    local targetBag = obj:FindFirstChild("Bag")
                    if targetBag then
                        local dist = (targetBag.Position - origin).Magnitude
                        if dist <= range then
                            table.insert(validTargets, {
                                Object = obj, Root = targetBag, Health = 100, IsModel = true, Priority = 1
                            })
                        end
                    end
                end
            end
        end

        if #validTargets > 0 then
            table.sort(validTargets, function(a, b)
                if a.Priority ~= b.Priority then
                    return a.Priority > b.Priority
                end
                if Toggles.MeleeAuraPriorityLow.Value then
                    return a.Health < b.Health
                elseif Toggles.MeleeAuraPriorityHigh.Value then
                    return a.Health > b.Health
                end
                return false
            end)

            currentMeleeTargetRoot = validTargets[1].Root
            local currentDamage = Toggles.CustomDamageToggle.Value and Options.CustomDamageAmount.Value or 50
            for _, target in ipairs(validTargets) do
                if target.IsModel then
                    WeaponsTargetsClient.hitTarget(target.Object, currentDamage)
                else
                    local fakeHumanoid = setmetatable({}, {
                        __index = function(_, key)
                            if key == "RootPart" then
                                return root
                            end
                            return target.Humanoid[key]
                        end,
                        __newindex = function(_, key, value)
                            target.Humanoid[key] = value
                        end
                    })
                    WeaponsDamageClient.dealMelee(target.Player, fakeHumanoid, currentDamage)
                end
            end
        else
            currentMeleeTargetRoot = nil
        end
    end
end)

task.spawn(function()
    task.wait(3)
    local effectModule = nil
    for _, obj in pairs(getgc(true)) do
        if type(obj) == "table" and rawget(obj, "runMuzzleEffects") and rawget(obj, "runFlintlockEffects") then
            effectModule = obj
            break
        end
    end
    if not effectModule then return end

    local targetEmitter = nil
    for _, upvalue in pairs(debug.getupvalues(effectModule.runFlintlockEffects)) do
        if type(upvalue) == "table" then
            local success, emitFunc = pcall(function() return upvalue.Emit end)
            if success and type(emitFunc) == "function" then
                targetEmitter = upvalue
                break
            end
        end
    end

    if targetEmitter then
        local originalEmit = targetEmitter.Emit
        targetEmitter.Emit = function(self, particleName, ...)
            if Toggles.NoSmoke and Toggles.NoSmoke.Value and type(particleName) == "string" and string.find(particleName, "Smoke") then
                return
            end
            return originalEmit(self, particleName, ...)
        end
    end
end)

SaveManager:LoadAutoloadConfig()

Library:OnUnload(function()
    if auraTask then task.cancel(auraTask) end
    for _, conn in ipairs(Connections) do conn:Disconnect() end
    if FOVCircle then FOVCircle:Remove() end
    if FOVFilledCircle then FOVFilledCircle:Remove() end
    if TracerLine then TracerLine:Remove() end
    if MeleeTargetTracer then MeleeTargetTracer:Remove() end
    for i = 1, 30 do
        if MeleeRangeLines[i] then MeleeRangeLines[i]:Remove() end
    end
    table.clear(drawingPropCache)
    Library.Unloaded = true
end)
